source code tamamen github'da; push ettigin andan itibaren otomatik olarak
compile edilip (windows, macos, linux) versiyonlari olusturuluyor.
github kendisi compile ediyor.

-------

cargo build --release

git tag v0.1.0
git push origin v0.1.0

# runtime'i bir yere kopyala.. ki kendini compile edildiginde bozulmasin, 
# kendini calisinca update edebilsin.

# make some changes
cargo build --release

git tag v0.1.1
git push origin v0.1.1

